{erl_opts, [debug_info]}.
{deps, [
{re_tuner, "0.1.5"}
]}.

{profiles, [
			{test, [{eunit_opts, [verbose]}]}
			]}.

{plugins, [rebar3_format]}.

{format, [
    {files, [
	"test/*.erl"
	
	]},
    {formatter, default_formatter},
    {options, #{ribbon=>80 }}   
]}.
