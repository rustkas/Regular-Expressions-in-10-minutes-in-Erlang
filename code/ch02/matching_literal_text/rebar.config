{erl_opts, [debug_info]}.
{deps, []}.


{profiles, [
			{test, [{eunit_opts, [verbose]}]}
			]}.

{plugins, [rebar3_format]}.

{format, [
    {files, [
	"test/*.erl"
	
	]},
    {formatter, default_formatter},
    {options, #{ribbon=>80 }}   
]}.
